use crate::robotics::Robot;

pub fn forward_dynamics(robot: &Robot) {


    // Compute the body inertia and joint axes, in global frames centered at the centers
    // of mass of the corresponding kinematic subtrees (to improve floating-point accuracy).

    // Compute the tendon lengths and moment arms. This includes the computation of
    // minimal-length paths for spatial tendons.

    // Compute the actuator lengths and moment arm.

    // Compute the reference constraint acceleration.

    // Compute the vector of Coriolis, centrifugal and gravitational forces.
}